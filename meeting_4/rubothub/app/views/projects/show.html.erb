<h2><%= @project.title %></h2>
<p>
  <%= @project.description %>
</p>

<ul>
  <% @project.users.each do |user| %>
    <li>
      <%= link_to "#{user.first_name} #{user.last_name}", user_path(user) %>
    </li>
  <% end %>
</ul>
